name: Claude Issue Implementation

on:
  issues:
    types: [opened, labeled]

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  implement:
    if: >-
      github.repository_owner == 'mattmacf98' &&
      (contains(github.event.issue.labels.*.name, 'claude') ||
      contains(github.event.issue.body, '@claude'))
    # viamrobotics/claude-ci-workflows@v1.14.0
    uses: viamrobotics/claude-ci-workflows/.github/workflows/claude-github-issue.yml@0a9af2603e5218af7448f59536a9f698b35d235f
    with:
      issue_number: ${{ github.event.issue.number }}
      title: ${{ github.event.issue.title }}
      body: ${{ github.event.issue.body }}
      repository_owner: mattmacf98
      install_command: npm ci
      show_full_output: true
      allowed_tools: 'Edit,Read,Write,Glob,Grep,Bash(make *),Bash(pnpm *),Bash(npx prettier *),Bash(npx svelte-kit *),Bash(npm run build*),Bash(npm run lint*),Bash(npm run test*),Bash(npm run sanity*),Bash(npm run typecheck*),Bash(npm run prettier*),Bash(git config *),Bash(git add *),Bash(git commit *),Bash(git push *),Bash(git status*),Bash(git diff*),Bash(git log*),Bash(git checkout *),Bash(git branch *),Bash(git rev-parse *),Bash(git fetch *)'
      extra_prompt: |
        - Run `npm run sanity` (typecheck + lint) and `npm run test` before committing.
        - All checks must pass. Fix any failures before creating the PR.
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
